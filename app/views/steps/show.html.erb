<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @step.name %>
</p>

<%= link_to 'Edit', edit_list_step_path(@list, @step) %> |
<%= link_to 'Back', list_path(@list) %>
<%= link_to 'Destroy', list_step_path(@list, @step), method: :delete, data: { confirm: 'Are you sure?' } %>
